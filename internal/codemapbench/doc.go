// Package codemapbench measures whether a missing-link generator can recover
// authored document-to-code links hidden from its input.
//
// The harness never recommends removal. Suggestions that do not match the
// current corpus are reported as unmatched, not treated as proof that a link
// is irrelevant.
package codemapbench
